-r requirements.txt
pytest>=7.4
pytest-mock>=3.10
ruff>=0.4
# Test-only: generates a real barcode image for test_barcode_photo.py's own
# decode round-trip — see #132. The app itself never encodes a barcode.
python-barcode>=0.15,<1.0
